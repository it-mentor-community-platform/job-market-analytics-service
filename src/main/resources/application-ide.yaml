server:
  port: 8086

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/it_mentor_community_platform?currentSchema=job_market_analytics_service
    username: root
    password: password
    driver-class-name: org.postgresql.Driver

  kafka:
    bootstrap-servers: localhost:29092